!!! 5
%html

  %head
    %meta(charset="utf-8")
    %title Welcome to Screenly

    %link(href="/static/css/bootstrap.css", rel="stylesheet")
    %link(href="/static/css/screenly.css", rel="stylesheet")

  %body.splash

    .container
      .row
        .span12
          .page-header.text-center
            %h1 FSG Display
	
	    .row
            .text-center
             %a(href="http://formulastudent.de/")
               %img(src="/static/img/fsglogo.jpg", alt="FSG")

      .row
        .span12.text-center
          - if ip_lookup:
            %p.lead To manage the content on this screen, just point your browser to:

            .well.lead
              %a(href="${url}") ${url}
          - else:
            %p.lead Unable to resolve management IP.

          .row
          .span12.text-center
           Based on Screenly-OSE  by 
           %a(href="http://wireload.net")
            Wireload
